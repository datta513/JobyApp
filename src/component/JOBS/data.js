/* import {Component} from 'react'

export default class Data extends Component {
  state = {present: '', sem: []}

  render() {
    const {salarydata, typedate, csalary, ctype} = this.props
    const {present, sem} = this.state
    console.log(present)
    const rendersalaryelemnt = data => {
      console.log(`data in render ${data}`)
      return <li key={data.id}>hello</li>
    }
  }
}
*/
